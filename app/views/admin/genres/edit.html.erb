<%= flash[:notice] %>
<h1>ジャンル編集</h1>

  <h2>ジャンル名</h2>
    <%= form_with model: @genre,url: admin_genre_path(@genre.id) do |f| %>
    <%= f.text_field :name %>
    <%= f.submit "変更を保存"%>
    <% end %>
